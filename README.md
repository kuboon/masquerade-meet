# 🎭 マスカレード

誰が誰だかわからない、オンライン仮面ミーティング。

Cloudflare の [Orange Meets](https://github.com/cloudflare/orange) をベースに、参加者ごとのボイスチェンジ機能を追加したものです。

- ルームを作るときに **キャラクターセット** を選びます。参加者は URL を開き、そのセットの **15 種類のキャラクター** から希望を 1 つ選びます（早い者勝ちではありません。希望が重なったらミーティング開始時に抽選）
- ロビーで **アンマスク後に表示する名前と画像** を登録します。名前を入れるまで準備完了にできません
- **2 人以上そろい、全員の準備が整うまで** ミーティングは始まりません。ルーム管理者が「ミーティング開始」を押すと、全員が同時に入室します
- ミーティング中は全員の **声色が選んだキャラクターの声に変わり**、カメラ映像の代わりに **キャラクターの絵** が表示されます。本名も表示されません
- ルーム管理者の指示で **5 秒のカウントダウン** ののち、ボイスチェンジが **一斉に解除** されます。同時に本名とカメラ映像が現れ、それまで被っていたキャラクターは各自の枠の隅に小さく残ります
- 顔を出したくない人は、入室時に **静止画** を登録しておけばカメラの代わりにそれが流れます。画像はブラウザから出ません

## しくみ

### ボイスチェンジ

`public/voice/voice-changer-worklet.js` の AudioWorklet が、マイクの音声をリアルタイムに変換します。

ピッチシフトは古典的な 2 タップ・ディレイライン方式です。リングバッファを 2 本の読み出しタップが追いかけ、その遅延量が `1 - pitchRatio` に比例して変化します。各タップはレイズドコサイン窓でクロスフェードされ、2 つの窓は半周期ずれているため常に合計がちょうど 1 になります。フォルマントもピッチと一緒に動くので、変装としてはむしろ好都合です。

その上に、キャラクターごとの個性としてビブラート LFO とリングモジュレーターを重ね、さらに main thread 側の 3 バンド EQ（`app/utils/voiceChanger.ts`）で音色を調整しています。

変換は **送信側のブラウザで** 行われるため、生の声は SFU にも他の参加者にも一切届きません。

パラメーターは `voiceParams$`（BehaviorSubject）経由で稼働中の AudioParam に流し込まれます。キャラクターの変更もマスクの解除も、トラックの再ネゴシエーションなしに音声グラフ上で完結します。

### 正体を隠す

本名はブラウザに送られません。Durable Object は `realName` を内部にだけ持ち、ブロードキャストする `name` にはキャラクター名を入れます。DevTools を覗いても正体は分かりません。

同じ理由で、`userUpdate` メッセージのうちクライアントが所有できるのはメディア状態（トラック、挙手、発話中）だけで、名前・キャラクター・準備状態・管理者権限はサーバー側が持ちます。

カメラは解除まで一度も配信されません（`getCamera({ broadcasting: false })`）。入室時に必要な権限も **マイクだけ** です。カメラは同じプロンプトでまとめて聞きますが、断られても、そもそも付いていない端末でも入室できます（`EnsurePermissions` がマイクのみで取り直します）。

### カウントダウンの同期

Durable Object は解除時刻 `revealAt` を自分の時計で決め、同じメッセージに `serverNow` を添えて配ります。各クライアントは差分 `revealAt - serverNow` だけを使って自分の時計上の期限を作るので、端末の時計がずれていてもカウントダウンはずれません。解除そのものはこの期限からのタイマーで発火するため、状態ブロードキャストが遅れても全員が同じ瞬間にマスクを外します。

### キャラクターセット

キャラクターは 15 体で 1 セットです。`app/utils/characterSets/` にセットごとのファイルがあり、`index.ts` がレジストリを持ちます。絵は `public/characters/<セット ID>/<キャラクター ID>.svg`、声のパラメーターは同じデータファイルの `voice` に書きます。

ルームが使うセットは **最初の接続で決まり、以後変わりません**。作成 URL の `?set=` を読んで Durable Object が固定するので、あとから参加する人は URL に何も付けなくても同じセットを見ます。逆に、すでに人がいるルームでは `?set=` は無視されます。会議の途中で全員の顔が挿げ替わらないためです。

セット ID とキャラクター ID は **一度デプロイしたら変更できません**。稼働中のルームは固定した ID を握っているので、改名するとキャラクターが解決できなくなります。

### キャラクターの抽選

ロビーでのキャラクター選択は「希望」であって「確保」ではありません。待っている間に「使用中」と断られるのは体験が悪く、先に押した人が得をするだけだからです。

希望が重なった場合は、ミーティング開始の瞬間に一度だけ抽選します（`app/utils/masquerade.ts` の `assignCharacters`）。順序をシャッフルしてから先着順に割り当て、外れた人には残っているキャラクターを配ります。シャッフルを挟まないとストレージの列挙順、つまり「先に接続した人」が毎回勝ってしまいます。乱数を差し替えられる純関数なので、公平さは Durable Object を立てずにテストできます。

### アンマスク後の名前と画像

名前と画像はロビーのキャラクター選択の下で登録します。専用の画面を挟まないのは、どちらもアンマスクされて初めて意味を持つ情報で、ルームの中で説明と一緒に聞くほうが分かりやすいからです。以前に名前を入れたことがある人が素通りしてしまい、画像を登録する機会がなくなる問題も避けられます。

名前は localStorage に覚えるので次回は入力済みになります。ルームへは打鍵のたびではなく少し間を置いてから送ります（1 打鍵ごとにルーム全体へ状態がブロードキャストされるため）。

Durable Object は接続時に名前を要求しません。座席は名無しで始まり、名前が入るまで `setReady` を拒否します。名前は他の参加者には送られず、`getPublicUsers` がアンマスクまでキャラクター名に差し替えます。

### 変装解除後の静止画

顔を出したくない人は、ロビー（または設定ダイアログ）で画像を登録できます。変装が解けたとき、カメラがオフならこの画像がカメラ映像の代わりに配信されます。

画像は **一度もサーバーに送られません**。localStorage に保存し、canvas に描いたものを `captureStream()` で普通のビデオトラックにして送ります。SFU はそれを他のカメラ映像と同じように中継するだけで、Durable Object にも D1 にも残りません。

partytracks の transform ではなく、送出する Observable そのものを切り替えています。transform はカメラが broadcasting のときしか通らないため、transform 方式だと「顔を映さないための機能」なのにウェブカメラを開いてランプを点けることになるからです。手動でカメラをオンにした場合はそちらが優先されます。

切り替えの条件に `camera.isBroadcasting$` を使うときは注意が要ります。これは partytracks の `broadcastTrack$` パイプラインの**内側から** `next()` されるので、`camera.broadcastTrack$` を購読するとこれが発火します。素直に書くと「購読 → 発火 → 条件が再 emit → switchMap が購読し直す」の無限ループになり、トラックが一度も下流に届きません。`distinctUntilChanged()` で値が変わったときだけ流すこと。

「カメラが動いているか」と「映像が出ているか」は別のフラグです（`videoEnabled` と `outgoingVideoEnabled`）。カメラボタンは前者を見て ON/OFF を切り替えるので、静止画で前者を true にすると、ボタンは「カメラを止める」しか提示しなくなり、カメラを一度も点けられなくなります。他の参加者に知らせるのは後者です。

この経路は実際のミーティングでしか目視できず（Realtime の認証情報が要る）、上記のループもそれで見逃されました。`/dev/still-image` はルームと同じ `outgoingVideoTrack$` を購読して再生するだけの開発用ページで、トラックが出ているか、フレームが実際に届いているかを認証情報なしに確認できます。フラグは observable ではなく `useUserMedia()` の返り値として表示しています（カメラボタンが読むのはそちらで、壊れたのもそちらだったため）。e2e もここを叩いています。

### 開発者用のボイス調整ツール

新しいセットの声を詰めるための画面が `/dev/voice` にあります（どこからもリンクしていません）。自分の声を 5 秒録音してループ再生しながらスライダーを動かし、決まった値を JSON でダウンロードするか、貼り付け用のスニペットをコピーしてデータファイルに書き戻します。

会議で使われるのと同じワークレットと EQ を、同じヘルパー（`app/utils/voiceChanger.ts`）から組み立てています。ここで聴こえた音と本番の音がずれないようにするためです。ただしノイズ抑制は通していません。

## セットアップ

Node.js 22 以上が必要です（Wrangler v4 の要件）。

[Cloudflare Realtime のダッシュボード](https://dash.cloudflare.com/?to=/:account/realtime)でアプリケーションを 1 つ作り、App ID と Secret を控えます。

```sh
npm install
cp .dev.vars.example .dev.vars   # 控えた 2 つの値を書き込む
```

`.dev.vars` はローカル開発とデプロイの両方で使う唯一の設定ファイルです（gitignore 済み）。

## 開発

```sh
npm run dev
```

[http://127.0.0.1:8787](http://127.0.0.1:8787) を開けば動きます。

チェック一式:

```sh
npm run check   # lint + typecheck + test
```

## デプロイ

```sh
npx wrangler login       # 初回だけ
npm run deploy           # ビルドしてデプロイ
npm run deploy:secrets   # 初回だけ。.dev.vars の値をシークレットとして投入
```

以降の更新は `npm run deploy` だけです。シークレットはデプロイでは消えないので、値を変えたときだけ `npm run deploy:secrets` を叩き直してください。

順番には理由があります。`wrangler secret bulk` は既存の Worker に対して実行するものなので、先に一度デプロイして Worker を作る必要があります。`.dev.vars` に書いた値はすべてシークレットとして送られるので、`OPENAI_API_TOKEN` などを足した場合もそのまま反映されます。

デプロイ先を確認するには:

```sh
npx wrangler deploy --dry-run   # 何も送らずにビルド内容だけ確認
npx wrangler tail               # 本番のログを流す
```

任意で [Cloudflare の TURN サービス](https://developers.cloudflare.com/calls/turn/)を使う場合は、`TURN_SERVICE_ID` と `TURN_SERVICE_TOKEN` を `.dev.vars` に足してから `npm run deploy:secrets` を実行してください。

### GitHub Actions で自動デプロイする

`main` に push すると自動でデプロイされるようにしてあります（`.github/workflows/ci.yml`）。リポジトリの **Settings → Secrets and variables → Actions** に次の 1 つを登録すれば有効になります。

| Secret                 | 取得先                                                                                                                     |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `CLOUDFLARE_API_TOKEN` | [API トークン](https://dash.cloudflare.com/profile/api-tokens)。**Edit Cloudflare Workers** テンプレートでそのまま作れます |

デプロイ先のアカウント ID（`CLOUDFLARE_ACCOUNT_ID`）はワークフローに直書きしてあります。アカウント ID は宛先を指すだけで単体では何の権限も持たないため、秘密にする必要はありません。フォークして自分のアカウントへデプロイする場合は、`.github/workflows/` の 2 つのファイルにある値を `npx wrangler whoami` で確認した自分の ID に差し替えてください。

Realtime の認証情報は Worker のシークレットとして一度入れれば十分なので、毎回のデプロイでは触りません。GitHub 側から入れ直したいときは `CALLS_APP_ID` と `CALLS_APP_SECRET` も Actions secrets に登録したうえで、**Actions → Sync Worker secrets → Run workflow** を手動で実行してください（`.github/workflows/sync-secrets.yml`）。ローカルから `npm run deploy:secrets` を叩くのと同じことをします。

## 既知の制限

- 1 ルームの定員は 15 人です（1 セットが 15 体のため）。それを超えるとミーティングを開始できません（「ミーティング開始」ボタンが理由を表示します）。
- 下限は 2 人です。1 人では変装して見せる相手がいないので、管理者ひとりのルームでは「ミーティング開始」を押せません。
- 画面共有は変装中も使えますが、共有した画面の内容から正体が分かる可能性があります。
- ページをリロードしてもキャラクターと準備状態は保たれます（`sessionStorage` の接続 ID による）。ただし 15 秒以上切断していると席が回収され、別のキャラクターになります。
- 変装解除は不可逆です。もう一度隠れるにはルームを作り直してください。

## ライセンス

Apache License 2.0（[LICENSE](./LICENSE)）。

本プロジェクトは [cloudflare/orange](https://github.com/cloudflare/orange)（Apache License 2.0, Copyright (c) 2024 Cloudflare, Inc.）の派生物です。帰属表示は [NOTICE](./NOTICE) にまとめてあります。
